"use strict";
exports.__esModule = true;
//instead of specfying string | number.
var person = function (userid, name) {
    console.log("" + userid);
};
var person1 = function (userid, name) {
    console.log(userid + "  " + name);
};
//instead of passing whole object you can pass this object.
